我们将整个buckyball仓库的设计分为俩部分，一个是具体芯片，一个是主线架构。
为什么这样分呢？呃，这鱼吧，黑猫的。设计与使用方式有关。
这仓库的主体都是属于主线架构部分，而examples下面是基于。嗯，不同版本仓库，呃，实现出来的具体的例子代码，其中部分经过了流片的验证。我们希望具体芯片设计与主线的架构。是可以解耦开来的架构主要包括很多方面，一切在DSC设计的过程中可以抽象出来共享的。部分都我们都将他认为是主线，具体的可能有仿真加速的工具链，比如bbox啊。 FPJ加速。我们为每一个设计去单独构建工具链是非常不划算的。而巴西木成通过建立统一抽象，让不同设计间可以共享同样的基础设施。编译器。使使基于ml的编译器，它将。呃，前端代码如常见的牌套是traitor或者，呃。呃，tail long。转换为。可执行文件。不同。具体的芯片。的指令集？是独立的。他们在共享的吧keyboard贝斯的基础上进行拓展。然后就是。芯片的RTL。部分。以验证框架为例。架构部分将会搭建。系统级。的。 UVM框架。而每个报内部复基于这个框架。保证报内部的覆盖率打满。

## Buckyball的设计哲学

由于我们系统架构是统一的，所以我们有一套统一的方法论，对。七系统的测试进行约定。 Firewall的架构设计基于几个原则，使得它足够。通用足够高效，适用于各种DLC的场景。首先。基于的一个盾，重要的抽象就是报仇向第二个很重要的抽象是半个抽象，我们将访存。追踪和操作的力度。设置为半壳。在一般的系统里头是比较粗粒度的一种表现。第三个我们。遵循little做设计了puppy board scaling philosophy。基于这些设计，我们构建了一个通用的架。我们默认。芯片的设计不应该修改主频架构。如果主线架构无法满足芯片设计需求，则应该有吧希望的架构框架进行迭代，以支持该功能。哎，具体的价格设计和具体的芯片介绍。可以分别阅读X和YY。感谢你看完整一大篇形而上学的介绍。


为了进一步阐释basketball的架构与设计分离的特点，我们逐个。我也饿，节后看bokee，鲍宇。具体。芯片设计之间的。分工。编译器方面，basketball提供了。一套ml框架。以及。这位LVM的维护。当你需要添加一个新的。设计的时候。选择你要的指报和指令构建你自己的hold LVM后端语。终端的dialect和path即可。对于工具。当你需要添加一个新的boss的时候。当你需要添加一个新的shirt的时候。对于RTL代码。当你需要添加一个新的包的时候，直接在。 Prototypes下。实现你的报。 Protect需要添加新的设计则参照parable。直接两颊即可。当你需要。由于框架是验证，我们默认。是验证完备的，所以每一个报的。设计，每个包的验证交给每个ball的开发单位在下面，而系统的。